{
  "name": "dromex-web",
  "version": "0.1.0",
  "private": true,
  "description": "DROMEX private web application workspace. Phase 1 foundation only.",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": "^22.12.0 || ^24.0.0"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
