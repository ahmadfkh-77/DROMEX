import type { FinancialOverview, FinancialTarget, OpeningBalanceDraft, PaymentDraft, PaymentEntry } from '../../domain/financials';
export interface FinancialRepository{getOverview():Promise<FinancialOverview>;createOpeningBalance(draft:OpeningBalanceDraft):Promise<FinancialTarget>;recordPayment(draft:PaymentDraft):Promise<FinancialTarget>;cancelPayment(paymentId:string,reason:string):Promise<FinancialTarget>;seedCustomerFinanceDemo():Promise<{customers:number;projects:number;loads:number;payments:number;openingBalances:number}>;removeCustomerFinanceDemo():Promise<{customers:number;projects:number;loads:number;payments:number;openingBalances:number}>;}
