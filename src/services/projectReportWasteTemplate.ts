import type {DailyProjectReport,LinkedProjectLoad,LinkedWasteDump,ProjectReportSetup,ReportProject} from '../domain/projectReports';
import {buildProjectReportHtml} from './documentTemplates';
const e=(value:unknown)=>String(value??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]??c));
export function buildProjectReportHtmlWithWaste(report:DailyProjectReport,project:ReportProject,loads:LinkedProjectLoad[],waste:LinkedWasteDump[],company:ProjectReportSetup['company'],logo:string|null,photos:(string|null)[]){const groups=[...waste.reduce((map,dump)=>{const key=`${dump.dumpLocation}\u0000${dump.materialType}`;map.set(key,{location:dump.dumpLocation,material:dump.materialType,count:(map.get(key)?.count??0)+1});return map;},new Map<string,{location:string;material:string;count:number}>()).values()];const rows=groups.map((v)=>`<tr><td>${e(v.material)}</td><td>${e(v.location)}</td><td>${v.count}</td></tr>`).join('')||'<tr><td colspan="3">No completed waste dumps</td></tr>';const section=`<section><h3>Waste dumps completed that day</h3><p><strong>Total completed dumps: ${waste.length}</strong></p><table><tr><th>Material</th><th>Dump location</th><th>Dumps</th></tr>${rows}</table></section>`;return buildProjectReportHtml(report,project,loads,company,logo,photos).replace('</body>',`${section}</body>`);}
