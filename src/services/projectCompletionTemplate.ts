import {netWorkMinutes,type DailyProjectReport,type ProjectCompletionLoad,type ProjectCompletionWasteDump,type ProjectReportSetup,type ReportProject} from '../domain/projectReports';

const e=(value:unknown)=>String(value??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]??c));
const display=(value:string|null|undefined)=>value?.trim()?e(value):'&mdash;';
const list=(values:string[])=>values.length?values.map(e).join(', '):'&mdash;';
const group=<T,>(values:T[],key:(value:T)=>string,amount:(value:T)=>number=()=>1)=>[...values.reduce((map,value)=>{const label=key(value);map.set(label,(map.get(label)??0)+amount(value));return map;},new Map<string,number>())].sort((a,b)=>b[1]-a[1]);
const niceDate=(value:string)=>{const match=/^(\d{4})-(\d{2})-(\d{2})$/.exec(value);return match?`${match[3]}/${match[2]}/${match[1]}`:value;};

export type ProjectCompletionPhoto={workDate:string;src:string};

export function buildProjectCompletionHtml(project:ReportProject,reports:DailyProjectReport[],loads:ProjectCompletionLoad[],waste:ProjectCompletionWasteDump[],company:ProjectReportSetup['company'],logo:string|null,photos:ProjectCompletionPhoto[]){
  const ordered=[...reports].sort((a,b)=>a.workDate.localeCompare(b.workDate));
  const orderedLoads=[...loads].sort((a,b)=>a.workDate.localeCompare(b.workDate)||a.transactionNumber.localeCompare(b.transactionNumber));
  const orderedWaste=[...waste].sort((a,b)=>a.workDate.localeCompare(b.workDate)||a.dumpedAt.localeCompare(b.dumpedAt));
  const dates=[...reports.map(v=>v.workDate),...loads.map(v=>v.workDate),...waste.map(v=>v.workDate)].sort();
  const start=dates[0]??'',end=dates[dates.length-1]??'';
  const workMinutes=reports.reduce((sum,report)=>sum+(netWorkMinutes({...report,id:report.id})??0),0);
  const issueDays=reports.filter(report=>{const value=report.problemsDelaysIncidents.trim();return value&&!/^no\b/i.test(value);}).length;
  const delivered=group(loads,v=>`${v.itemName}\u0000${v.unitSymbol}`,v=>v.quantity);
  const materials=group(reports.flatMap(v=>v.materials),v=>`${v.movement}\u0000${v.itemName}\u0000${v.unitSymbol}`,v=>v.quantity);
  const byMaterial=group(waste,v=>v.materialType),byLocation=group(waste,v=>v.dumpLocation);
  const workers=group(reports.flatMap(v=>v.workers),v=>v),drivers=group(reports.flatMap(v=>v.drivers),v=>v);
  const trucks=group(reports.flatMap(v=>v.truckPlates),v=>v),machines=group(reports.flatMap(v=>v.machines),v=>v);
  const summaryRows=(values:[string,number][],split=false)=>values.map(([key,value])=>{const parts=key.split('\u0000');return `<tr>${parts.map(part=>`<td>${e(part)}</td>`).join('')}<td class="number">${split?value.toFixed(3):value}</td></tr>`;}).join('');
  const attendanceRows=(values:[string,number][],empty:string)=>values.map(([name,days])=>`<tr><td>${e(name)}</td><td class="number">${days}</td></tr>`).join('')||`<tr><td colspan="2" class="empty">${empty}</td></tr>`;
  const daySections=ordered.map((report,index)=>{
    const dayLoads=loads.filter(v=>v.workDate===report.workDate),dayWaste=waste.filter(v=>v.workDate===report.workDate);
    const minutes=netWorkMinutes({...report,id:report.id});
    const dayPhotos=photos.filter(v=>v.workDate===report.workDate).length;
    const materialRows=report.materials.map(material=>`<tr><td>${e(material.itemName)}</td><td>${e(material.movement)}</td><td class="number">${material.quantity.toFixed(3)} ${e(material.unitSymbol)}</td></tr>`).join('');
    return `<article class="day-card"><div class="day-heading"><div><span class="day-index">REPORT ${String(index+1).padStart(2,'0')}</span><h3>${e(niceDate(report.workDate))}</h3></div><div class="day-stats"><span>${minutes==null?'No time':`${(minutes/60).toFixed(1)} h net`}</span><span>${dayLoads.length} load${dayLoads.length===1?'':'s'}</span><span>${dayWaste.length} dump${dayWaste.length===1?'':'s'}</span><span>${dayPhotos} photo${dayPhotos===1?'':'s'}</span></div></div><div class="work-block"><span class="field-label">WORK COMPLETED</span><p>${display(report.workDescription)}</p></div><div class="detail-grid"><div><span class="field-label">Working time</span><p>${display(report.workStartTime)} to ${display(report.workEndTime)}${report.breakMinutes?` &middot; ${e(report.breakMinutes)} min break`:''}</p></div><div><span class="field-label">Weather / site conditions</span><p>${display(report.weatherSiteConditions)}</p></div><div><span class="field-label">Workers and drivers</span><p>${list([...report.workers,...report.drivers])}</p></div><div><span class="field-label">Trucks and machines</span><p>${list([...report.truckPlates,...report.machines])}</p></div></div>${materialRows?`<table class="day-materials"><thead><tr><th>Material</th><th>Movement</th><th class="number">Quantity</th></tr></thead><tbody>${materialRows}</tbody></table>`:''}<div class="day-notes"><div><span class="field-label">SITE NOTES</span><p>${display(report.notes)}</p></div><div class="issue"><span class="field-label">PROBLEMS / DELAYS / INCIDENTS</span><p>${display(report.problemsDelaysIncidents)}</p></div><div><span class="field-label">NEXT WORK PLANNED</span><p>${display(report.nextWorkPlanned)}</p></div></div></article>`;
  }).join('')||'<div class="empty-panel">No daily reports were recorded for this project.</div>';
  const loadRows=orderedLoads.map(v=>`<tr><td>${e(niceDate(v.workDate))}</td><td>${e(v.transactionNumber)}</td><td>${e(v.itemName)}</td><td class="number">${v.quantity.toFixed(3)} ${e(v.unitSymbol)}</td><td>${e(v.driverName)}</td><td>${e(v.truckPlate)}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">No project-linked loads</td></tr>';
  const wasteRows=orderedWaste.map(v=>`<tr><td>${e(niceDate(v.workDate))}</td><td>${e(new Date(v.dumpedAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}))}</td><td>${e(v.materialType)}</td><td>${e(v.dumpLocation)}</td><td>${display(v.truckPlate)}</td><td>${display(v.driverName)}</td></tr>`).join('')||'<tr><td colspan="6" class="empty">No active waste dumps</td></tr>';
  const photoHtml=photos.map((photo,index)=>`<figure><img src="${photo.src}"/><figcaption>${e(niceDate(photo.workDate))} &middot; Photo ${index+1}</figcaption></figure>`).join('');
  const generated=e(new Date().toLocaleString());
  return `<!doctype html><html><head><meta charset="utf-8"><style>
  @page{size:A4;margin:16mm 13mm 18mm}*{box-sizing:border-box}body{margin:0;font-family:Arial,sans-serif;color:#172a3a;font-size:9pt;line-height:1.42}.cover{min-height:246mm;display:flex;flex-direction:column}.header{display:flex;justify-content:space-between;align-items:flex-start;gap:10mm;border-bottom:3px solid #d3542f;padding-bottom:5mm}.brand{max-width:92mm}.logo{max-width:38mm;max-height:21mm}.company-name{font-size:18pt;font-weight:800;color:#173f67;margin:0 0 1mm}.company-contact{color:#637586;font-size:8pt}.report-title{text-align:right}.report-title .kicker,.section-kicker,.day-index{display:block;color:#d3542f;font-size:7.5pt;font-weight:800;letter-spacing:1.2px}.report-title h1{font-size:22pt;line-height:1.06;color:#173f67;margin:1mm 0}.status{display:inline-block;background:#e7f5ed;color:#25724a;border-radius:20px;padding:1.2mm 3mm;font-size:8pt;font-weight:700}.project-hero{margin:14mm 0 7mm;border-left:5px solid #d3542f;padding:2mm 0 2mm 6mm}.project-hero h2{font-size:21pt;line-height:1.15;margin:0 0 2mm;color:#173f67}.project-hero p{margin:0;color:#637586;font-size:11pt}.project-meta{display:grid;grid-template-columns:1fr 1fr;gap:4mm;background:#f4f7f9;border:1px solid #dfe6eb;border-radius:3mm;padding:5mm}.meta-item{min-height:12mm}.field-label{display:block;color:#637586;font-size:7.3pt;font-weight:800;letter-spacing:.45px;text-transform:uppercase;margin-bottom:.7mm}.meta-value{font-size:10pt;font-weight:700}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:3mm;margin:6mm 0}.metric{border:1px solid #dfe6eb;border-top:3px solid #2e78a6;border-radius:2mm;padding:3.5mm;background:#fff}.metric:nth-child(2n){border-top-color:#d3542f}.metric strong{display:block;font-size:19pt;line-height:1;color:#173f67;margin-bottom:1mm}.metric span{color:#637586;font-size:8pt}.cover-note{margin-top:auto;padding-top:6mm;border-top:1px solid #dfe6eb;color:#637586;font-size:8pt}.section-page{page-break-before:always}.section-head{display:flex;align-items:flex-end;justify-content:space-between;border-bottom:3px solid #173f67;padding-bottom:3mm;margin-bottom:5mm}.section-head h2{font-size:18pt;color:#173f67;margin:0}.section-head p{margin:0;color:#637586;font-size:8pt}.subsection{margin:0 0 7mm;break-inside:avoid}.subsection h3{font-size:11pt;color:#173f67;margin:0 0 2.5mm;padding-bottom:1.5mm;border-bottom:1px solid #ccd7df}.two-col{display:grid;grid-template-columns:1fr 1fr;gap:6mm}table{width:100%;border-collapse:collapse;font-size:8.2pt}thead{display:table-header-group}tr{break-inside:avoid}th{background:#eaf1f6;color:#274c68;text-transform:uppercase;letter-spacing:.3px;font-size:7.2pt}th,td{text-align:left;padding:2mm;border:1px solid #dfe6eb;vertical-align:top}tbody tr:nth-child(even){background:#f8fafb}.number{text-align:right;white-space:nowrap}.empty{color:#637586;text-align:center;font-style:italic}.day-card{border:1px solid #d7e0e7;border-radius:3mm;margin-bottom:6mm;break-inside:avoid;overflow:hidden}.day-heading{background:#173f67;color:#fff;padding:3mm 4mm;display:flex;justify-content:space-between;align-items:center;gap:4mm}.day-heading h3{font-size:13pt;margin:.5mm 0 0;color:#fff}.day-heading .day-index{color:#f3a183}.day-stats{display:flex;flex-wrap:wrap;justify-content:flex-end;gap:1.5mm}.day-stats span{background:#28577d;border-radius:12px;padding:1mm 2.2mm;font-size:7.5pt;white-space:nowrap}.work-block{padding:4mm 4mm 2mm}.work-block p{font-size:10pt;font-weight:700;margin:1mm 0}.detail-grid{display:grid;grid-template-columns:1fr 1fr;gap:3mm 6mm;padding:2mm 4mm 4mm}.detail-grid p,.day-notes p{margin:0;white-space:pre-wrap}.day-materials{width:calc(100% - 8mm);margin:0 4mm 4mm}.day-notes{display:grid;grid-template-columns:1fr 1fr 1fr;gap:0;border-top:1px solid #dfe6eb;background:#f8fafb}.day-notes>div{padding:3mm;border-right:1px solid #dfe6eb}.day-notes>div:last-child{border-right:0}.day-notes .issue{background:#fff8f4}.appendix-table{font-size:7.8pt}.empty-panel{padding:8mm;background:#f4f7f9;border:1px solid #dfe6eb;text-align:center;color:#637586}.photos{display:grid;grid-template-columns:1fr 1fr;gap:5mm}.photos figure{margin:0;break-inside:avoid;border:1px solid #dfe6eb;padding:2mm}.photos img{width:100%;height:92mm;object-fit:contain;background:#f4f7f9}.photos figcaption{font-size:8pt;color:#637586;padding-top:1.5mm}
  @page{size:A4;margin:18mm 17mm 20mm}
  </style></head><body>
  <main class="cover"><header class="header"><div class="brand">${logo?`<img class="logo" src="${logo}"/>`:`<div class="company-name">${e(company.name)}</div>`}<div class="company-contact">${display(company.address)}${company.phone?` &middot; ${e(company.phone)}`:''}${company.email?` &middot; ${e(company.email)}`:''}</div></div><div class="report-title"><span class="kicker">FINAL RECORD</span><h1>Project Completion<br/>Report</h1><span class="status">COMPLETED</span></div></header><div class="project-hero"><h2>${e(project.name)}</h2><p>${e(company.name)} &middot; ${e(project.location)}</p></div><div class="project-meta"><div class="meta-item"><span class="field-label">Contractor</span><div class="meta-value">${e(company.name)}</div></div><div class="meta-item"><span class="field-label">Project location</span><div class="meta-value">${e(project.location)}</div></div><div class="meta-item"><span class="field-label">Recorded start</span><div class="meta-value">${start?e(niceDate(start)):'&mdash;'}</div></div><div class="meta-item"><span class="field-label">Recorded completion</span><div class="meta-value">${end?e(niceDate(end)):'&mdash;'}</div></div></div><div class="metrics"><div class="metric"><strong>${reports.length}</strong><span>Daily reports</span></div><div class="metric"><strong>${loads.length}</strong><span>Delivered loads</span></div><div class="metric"><strong>${waste.length}</strong><span>Waste dumps</span></div><div class="metric"><strong>${(workMinutes/60).toFixed(1)}</strong><span>Recorded net hours</span></div><div class="metric"><strong>${photos.length}</strong><span>Project photos</span></div><div class="metric"><strong>${issueDays}</strong><span>Days with issues</span></div></div><div class="cover-note">This report consolidates the project's recorded daily work, labor, equipment, materials, delivered loads, waste movements, site conditions, issues and photographic evidence. Generated ${generated}.</div></main>
  <section class="section-page"><div class="section-head"><div><span class="section-kicker">SECTION 01</span><h2>Operational Summary</h2></div><p>Start-to-finish recorded totals</p></div><div class="subsection"><h3>Delivered materials</h3><table><thead><tr><th>Material</th><th>Unit</th><th class="number">Total quantity</th></tr></thead><tbody>${summaryRows(delivered,true)||'<tr><td colspan="3" class="empty">No delivered materials</td></tr>'}</tbody></table></div><div class="subsection"><h3>Materials entered in daily reports</h3><table><thead><tr><th>Movement</th><th>Material</th><th>Unit</th><th class="number">Total quantity</th></tr></thead><tbody>${summaryRows(materials,true)||'<tr><td colspan="4" class="empty">No daily-report materials</td></tr>'}</tbody></table></div><div class="subsection"><h3>Waste disposal summary</h3><div class="two-col"><table><thead><tr><th>Material</th><th class="number">Dumps</th></tr></thead><tbody>${summaryRows(byMaterial)||'<tr><td colspan="2" class="empty">No waste dumps</td></tr>'}</tbody></table><table><thead><tr><th>Dump location</th><th class="number">Dumps</th></tr></thead><tbody>${summaryRows(byLocation)||'<tr><td colspan="2" class="empty">No waste dumps</td></tr>'}</tbody></table></div></div></section>
  <section class="section-page"><div class="section-head"><div><span class="section-kicker">SECTION 02</span><h2>People and Equipment</h2></div><p>Number of recorded report days</p></div><div class="two-col"><div class="subsection"><h3>Workers</h3><table><thead><tr><th>Name</th><th class="number">Days</th></tr></thead><tbody>${attendanceRows(workers,'No workers recorded')}</tbody></table></div><div class="subsection"><h3>Drivers</h3><table><thead><tr><th>Name</th><th class="number">Days</th></tr></thead><tbody>${attendanceRows(drivers,'No drivers recorded')}</tbody></table></div><div class="subsection"><h3>Trucks</h3><table><thead><tr><th>Plate</th><th class="number">Days</th></tr></thead><tbody>${attendanceRows(trucks,'No trucks recorded')}</tbody></table></div><div class="subsection"><h3>Machines</h3><table><thead><tr><th>Machine</th><th class="number">Days</th></tr></thead><tbody>${attendanceRows(machines,'No machines recorded')}</tbody></table></div></div></section>
  <section class="section-page"><div class="section-head"><div><span class="section-kicker">SECTION 03</span><h2>Chronological Site Diary</h2></div><p>${reports.length} daily report${reports.length===1?'':'s'}</p></div>${daySections}</section>
  <section class="section-page"><div class="section-head"><div><span class="section-kicker">APPENDIX A</span><h2>Delivered Loads</h2></div><p>${loads.length} project-linked load${loads.length===1?'':'s'}</p></div><table class="appendix-table"><thead><tr><th>Date</th><th>Receipt</th><th>Material</th><th class="number">Quantity</th><th>Driver</th><th>Truck</th></tr></thead><tbody>${loadRows}</tbody></table></section>
  <section class="section-page"><div class="section-head"><div><span class="section-kicker">APPENDIX B</span><h2>Waste Dump History</h2></div><p>${waste.length} active dump record${waste.length===1?'':'s'}</p></div><table class="appendix-table"><thead><tr><th>Date</th><th>Time</th><th>Material</th><th>Location</th><th>Truck</th><th>Driver</th></tr></thead><tbody>${wasteRows}</tbody></table></section>
  ${photoHtml?`<section class="section-page"><div class="section-head"><div><span class="section-kicker">APPENDIX C</span><h2>Project Photos</h2></div><p>${photos.length} photo${photos.length===1?'':'s'}</p></div><div class="photos">${photoHtml}</div></section>`:''}</body></html>`;
}
